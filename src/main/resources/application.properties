#DB
# Values -> create, create-drop, validate, update, none, drop
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.url=jdbc:postgresql://localhost:5432/practicas
spring.datasource.username=dam2
spring.datasource.password=1234
spring.jpa.properties.hibernate.default_schema=spring_data
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# Run the queries from the data.sql file once the DB schema has been created
#spring.jpa.defer-datasource-initialization=true

# Execution of data.sql queries.
#spring.sql.init.mode=always